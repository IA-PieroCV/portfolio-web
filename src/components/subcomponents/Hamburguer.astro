<button
  class="my-auto justify-center items-center md:hidden flex flex-col gap-2 hamburger w-10"
>
  <span class="bg-white w-10 h-1 rounded-full"></span>
  <span class="bg-white w-10 h-1 rounded-full"></span>
  <span class="bg-white w-10 h-1 rounded-full"></span>
</button>

<style>
  .hamburger.active span:nth-child(1) {
    transform: translateY(0.75rem) rotate(45deg);
  }

  .hamburger.active span:nth-child(2) {
    opacity: 0;
  }

  .hamburger.active span:nth-child(3) {
    transform: translateY(-0.75rem) rotate(-45deg);
  }

  .hamburger span {
    transition: transform 0.3s ease;
  }
</style>
